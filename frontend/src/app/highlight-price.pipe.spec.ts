import { HighlightPricePipe } from './highlight-price.pipe';

describe('HighlightPricePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightPricePipe();
    expect(pipe).toBeTruthy();
  });
});
